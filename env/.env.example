# =============================================================================
# AUTHORFLOW STUDIOS - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real secrets to version control
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE (Database & Auth)
# -----------------------------------------------------------------------------
SUPABASE_URL=                          # Your Supabase project URL (e.g., https://xxxxx.supabase.co)
SUPABASE_ANON_KEY=                     # Public anon key for frontend (safe to expose)
SUPABASE_SERVICE_ROLE_KEY=             # Service role key for backend ONLY (never expose to frontend!)

# Deprecated: Supabase Storage (now using Cloudflare R2 instead)
# SUPABASE_BUCKET_MANUSCRIPTS=manuscripts
# SUPABASE_BUCKET_AUDIOBOOKS=audiobooks

# -----------------------------------------------------------------------------
# CLOUDFLARE R2 STORAGE (S3-Compatible)
# -----------------------------------------------------------------------------
# Get credentials from: Cloudflare Dashboard → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=                         # Cloudflare account ID (found in R2 overview)
R2_ACCESS_KEY_ID=                      # R2 Access Key ID (from API token)
R2_SECRET_ACCESS_KEY=                  # R2 Secret Access Key (from API token)

# R2 Bucket Names
R2_BUCKET_MANUSCRIPTS=manuscripts      # Bucket for manuscript files (TXT, DOCX, PDF)
R2_BUCKET_AUDIOBOOKS=audiobooks        # Bucket for generated audiobook files (MP3, WAV, etc.)

# R2 Endpoints
R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com  # Replace <ACCOUNT_ID> with your R2_ACCOUNT_ID
R2_PUBLIC_URL=https://pub-<hash>.r2.dev                    # Optional: R2 public URL or custom domain

# -----------------------------------------------------------------------------
# TTS API KEYS (Backend Only)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=                        # OpenAI API key for TTS-1 (primary provider)
ELEVENLABS_API_KEY=                    # ElevenLabs API key (optional, higher quality)

# Inworld TTS Configuration (optional - all 4 fields required if using Inworld)
# Get from: https://studio.inworld.ai/ → Settings → API Credentials
INWORLD_WORKSPACE_ID=                  # Inworld workspace ID (e.g., workspaces/default-1234567890)
INWORLD_JWT_KEY=                       # Inworld JWT Key (from API credentials page)
INWORLD_JWT_SECRET=                    # Inworld JWT Secret (from API credentials page)
INWORLD_BASIC_AUTH=                    # Inworld Basic Auth (Base64 encoded workspace_key:api_key)

# -----------------------------------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------------------------------
NEXT_PUBLIC_ENGINE_API_URL=https://your-backend.railway.app              # Public URL to Railway backend API
FRONTEND_URL=https://authorflowstudios.rohimayapublishing.com           # Production frontend URL

# CORS Configuration
ALLOWED_ORIGINS=https://authorflowstudios.rohimayapublishing.com,http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# GENERAL CONFIGURATION
# -----------------------------------------------------------------------------
LOG_LEVEL=info                         # Logging level: debug | info | warn | error
ENVIRONMENT=development                # Environment: development | staging | production

# -----------------------------------------------------------------------------
# PROCESSING SETTINGS (Optional - Defaults provided in code)
# -----------------------------------------------------------------------------
CHUNK_SIZE=1500                        # Text chunk size in characters (default: 1500)
MAX_WORKERS=5                          # Maximum parallel workers for TTS (default: 5)
RATE_LIMIT_RPM=60                      # API rate limit in requests per minute (default: 60)

# -----------------------------------------------------------------------------
# GOOGLE DRIVE INTEGRATION (Future/Optional)
# -----------------------------------------------------------------------------
# GOOGLE_DRIVE_CLIENT_ID=              # Google OAuth client ID (future use)
# GOOGLE_DRIVE_CLIENT_SECRET=          # Google OAuth client secret (future use)

# -----------------------------------------------------------------------------
# DATABASE CONNECTION (If using direct Postgres instead of Supabase SDK)
# -----------------------------------------------------------------------------
# DATABASE_URL=                        # Direct Postgres connection string (optional)

# =============================================================================
# END OF ENVIRONMENT VARIABLES
# =============================================================================
